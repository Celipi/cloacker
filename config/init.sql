   -- Garantir que o banco de dados existe
   CREATE DATABASE IF NOT EXISTS cloacker;

   -- Configurar algumas permissões ou configurações globais, se necessário
   -- ALTER DATABASE cloacker SET timezone TO 'UTC';
